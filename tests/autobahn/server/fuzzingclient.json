{
    "options": { "failByDrop": false },
    "outdir": "./tests/autobahn/reports/servers",

    "servers": [
        {
            "agent": "AutobahnServer",
            "url": "ws://localhost:9001",
            "options": { "version": 18 }
        }
    ],

    "cases": ["*"],
    "exclude-cases": ["12.*", "13.*"],
    "exclude-agent-cases": {}
}
