name: Pull-Request

on:
  pull_request:
    branches:
      - 'master'
      - '[0-9].[0-9]+'

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number }}
  cancel-in-progress: true


jobs:
  compile-deps:
    name: Compile deps
    if: ${{ github.actor != 'dependabot[bot]' }}
    uses: aio-libs/aiohttp/.github/workflows/compile-deps.yml@dd7a391876aa03721720f3fb0fd075462d576347
    secrets:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # TODO: replace to @master after the PR merging
  lint:
    name: Lint
    needs: compile-deps
    uses: aio-libs/aiohttp/.github/workflows/lint.yml@dd7a391876aa03721720f3fb0fd075462d576347
  test:
    name: Test
    needs: lint
    uses: aio-libs/aiohttp/.github/workflows/test.yml@dd7a391876aa03721720f3fb0fd075462d576347

  check:  # This job does nothing and is only used for the branch protection
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Report success of the test matrix
        run: >-
          print("All's good")
        shell: python
