Narrowed duplicate singleton header rejection to only ``Host``, ``Content-Length``,
and ``Transfer-Encoding`` -- the headers where duplicates have real security
implications (request smuggling, host header attacks). Other RFC 9110 singletons
like ``Content-Type``, ``Server``, and ``User-Agent`` are no longer rejected, as
real-world servers commonly send them -- by :user:`bdraco`.
