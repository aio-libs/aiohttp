Stopped the ``Parameters:``, ``Returns:`` and similar Sphinx field labels in
the rendered documentation from collapsing into a very narrow column by
giving ``dl.field-list>dt`` a small ``min-width`` in the custom CSS.
