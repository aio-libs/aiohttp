Fixed unbounded memory growth on a client WebSocket connection after a frame
protocol error. The reader is detached by the error but the connection stays
upgraded, so a peer could stream unlimited data into an internal buffer when
the application never called
:meth:`~aiohttp.ClientWebSocketResponse.receive`. Data arriving after the
error is now discarded, since nothing can parse it
-- by :user:`bdraco`.
