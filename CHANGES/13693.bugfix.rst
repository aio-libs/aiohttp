Fixed :meth:`~aiohttp.web.Application.add_domain` not routing a request to
its domain application when the ``Host`` header carried a trailing dot or
an explicit default port, so the request fell through to the parent
application's routes -- by :user:`rodrigobnogueira`.
