Fixed the WebSocket client handshake to parse the response ``Connection`` header as a comma-separated token list per :rfc:`9110#section-7.6.1`. Responses such as ``Connection: upgrade, keep-alive`` are now accepted, and values like ``keep-alive, notupgrade`` are correctly rejected -- by :user:`rodrigobnogueira`.
