Fixed the WebSocket client handshake to parse the response ``Connection`` header as a comma-separated token list per :rfc:`9110#section-7.6.1`, so spec-compliant responses such as ``Connection: upgrade, keep-alive`` are now accepted instead of being rejected -- by :user:`rodrigobnogueira`.
