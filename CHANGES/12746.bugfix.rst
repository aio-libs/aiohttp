Fixed the WebSocket client handshake rejecting spec-compliant responses whose ``Connection`` header lists ``upgrade`` alongside other tokens, such as ``Connection: upgrade, keep-alive`` (:rfc:`9110#section-7.6.1`) -- by :user:`rodrigobnogueira`.
