Reassembled a WebSocket frame delivered across many small reads into a single growable buffer, keeping retained memory proportional to the frame rather than one object per read -- by :user:`Dreamsorcerer` and :user:`bdraco`.
