Fixed "Future exception was never retrieved" warning spam when a request handler task is cancelled during TCP write backpressure. :meth:`BaseProtocol._drain_helper` now awaits ``_drain_waiter`` directly instead of wrapping it in :func:`asyncio.shield`, so task cancellation propagates to the waiter itself and ``connection_lost`` correctly skips ``set_exception`` when the waiter is already done -- by :user:`NIK-TIGER-BILL`.